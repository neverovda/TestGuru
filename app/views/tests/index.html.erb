<h1><%= t('.header') %></h1>

<div class="row mt-3">
  <table class="table">
     <tr class="table-primary">
      <th><%= t('.title') %></th>
      <th><%= t('.level') %></th>
      <th><%= t('.category') %></th>
      <th><%= t('.author') %></th>
      <th><%= t('.quantity_questins') %></th>
      <th><%= t('action') %></th>
    </tr>

    <%= render @tests %>

    <% unless @tests.present? %>
      <th colspan="6"><%= t('.no_tests') %></th>  
    <% end %>
  </table>
</div>  
