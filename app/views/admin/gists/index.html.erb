<h1><%= t('.header') %></h1>

<p>
  <%= link_to t('.admin_test_view'), admin_tests_path, class: 'btn btn-primary' %>  
</p>

<div class="row mt-3">
  <table class="table">
     <tr class="table-primary">
      <th><%= t('.question') %></th>
      <th>Gist Url</th>
      <th><%= t('.user_email') %></th>
    </tr>

    <%= render @gists %>

    <% unless @gists.present? %>
      <th colspan="3"><%= t('.no_gist') %></th>  
    <% end %>
  </table>
</div>  
